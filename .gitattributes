# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Godot Web export output — never line-ending convert these
*.wasm binary
*.pck  binary
web/*.js  -text
web/*.png binary
